/**
 * HOW TO EDIT
 * Replace any '[[VALUE]]' with the real URL or address. Keep the quotes.
 * Until you do, anything that depends on that link is hidden from visitors
 * (and shows a red TODO chip while running `npm run dev`).
 *
 * email is a plain address, not a mailto: link — the code adds mailto: itself.
 */

export const links = {
  apply: "[[GOOGLE_FORM_URL]]",
  newsletter: "[[NEWSLETTER_SIGNUP_URL]]",
  email: "[[CLUB_EMAIL]]",
  instagram: "https://instagram.com/theforwardsocietyru",
} as const;

export const instagramHandle = "@theforwardsocietyru";
